# getSVGSize 双向传值完整示例

## 📋 概述

增强版的 `getSVGSize` 接口展示了完整的 **JavaScript ↔ C++ 双向传值**，包括各种数据类型的传递和转换。

---

## 🔄 双向传值流程

```
┌─────────────────────────────────────────────────────────────┐
│  JavaScript 层                                               │
│  ───────────────────────────────────────────────────────────│
│  const options = {                                          │
│    unit: 'px',                    // string                │
│    scale: 1.5,                    // number                │
│    includeAspectRatio: true,      // boolean               │
│    extraInfo: ['info1', 'info2'], // array                 │
│    metadata: { ... }              // nested object         │
│  };                                                         │
│  const result = module.getSVGSize(tag, options);          │
└───────────────────────┬─────────────────────────────────────┘
                        │ JavaScript → C++
                        ▼
┌─────────────────────────────────────────────────────────────┐
│  桥接函数（参数解析）                                        │
│  ───────────────────────────────────────────────────────────│
│  args[0] → tag (number → int32_t)                          │
│  args[1] → options (object → jsi::Object)                  │
│    - unit: string → std::string                            │
│    - scale: number → double                                 │
│    - includeAspectRatio: boolean → bool                    │
│    - extraInfo: array → std::vector<string>                │
│    - metadata: object → jsi::Object                         │
└───────────────────────┬─────────────────────────────────────┘
                        │ 调用 C++ 函数
                        ▼
┌─────────────────────────────────────────────────────────────┐
│  C++ 函数（业务逻辑）                                        │
│  ───────────────────────────────────────────────────────────│
│  1. 读取 JavaScript 传递的值                                │
│  2. 执行业务逻辑                                             │
│  3. 创建返回对象                                             │
└───────────────────────┬─────────────────────────────────────┘
                        │ C++ → JavaScript
                        ▼
┌─────────────────────────────────────────────────────────────┐
│  桥接函数（返回值转换）                                      │
│  ───────────────────────────────────────────────────────────│
│  jsi::Object → jsi::Value                                  │
│    - width: double → number                                │
│    - height: double → number                               │
│    - success: bool → boolean                               │
│    - sizeString: std::string → string                      │
│    - sizeInfo: jsi::Object → nested object                │
│    - dimensions: jsi::Array → array                       │
│    - metadata: jsi::Object → object                        │
└───────────────────────┬─────────────────────────────────────┘
                        │ 返回结果
                        ▼
┌─────────────────────────────────────────────────────────────┐
│  JavaScript 层                                               │
│  ───────────────────────────────────────────────────────────│
│  const result = {                                           │
│    width: 150,                  // number                  │
│    height: 150,                 // number                  │
│    success: true,               // boolean                 │
│    sizeString: '150x150px',     // string                 │
│    sizeInfo: { ... },           // nested object           │
│    dimensions: [150, 150],      // array                   │
│    metadata: { ... }            // object                   │
│  };                                                         │
└─────────────────────────────────────────────────────────────┘
```

---

## 📤 JavaScript → C++ 传值

### 传递的参数

```typescript
// 参数 1: tag (number)
const tag = 1;

// 参数 2: options (object)
const options = {
  // 1. 字符串类型 (string)
  unit: 'px',
  
  // 2. 数字类型 (number)
  scale: 1.5,
  
  // 3. 布尔类型 (boolean)
  includeAspectRatio: true,
  
  // 4. 数组类型 (array)
  extraInfo: ['info1', 'info2', 'info3'],
  
  // 5. 嵌套对象 (nested object)
  metadata: {
    source: 'react-native-svg',
    version: '1.0.0'
  }
};

// 调用
const result = RNSVGSvgViewModule.getSVGSize(tag, options);
```

### C++ 端解析

```cpp
// 1. 解析 tag (number → int32_t)
auto tag = args[0].asNumber();  // JavaScript number → C++ int32_t

// 2. 解析 options (object → jsi::Object)
jsi::Object opts = args[1].getObject(rt);

// 3. 读取字符串属性 (string → std::string)
std::string unit = opts.getProperty(rt, "unit").asString(rt).utf8(rt);

// 4. 读取数字属性 (number → double)
double scale = opts.getProperty(rt, "scale").asNumber();

// 5. 读取布尔属性 (boolean → bool)
bool includeAR = opts.getProperty(rt, "includeAspectRatio").getBool();

// 6. 读取数组属性 (array → std::vector)
jsi::Array infoArray = opts.getProperty(rt, "extraInfo").asArray(rt);
for (size_t i = 0; i < infoArray.length(rt); i++) {
    std::string item = infoArray.getValueAtIndex(rt, i).asString(rt).utf8(rt);
    extraInfo.push_back(item);
}

// 7. 读取嵌套对象 (object → jsi::Object)
jsi::Object metadata = opts.getProperty(rt, "metadata").asObject(rt);
std::string source = metadata.getProperty(rt, "source").asString(rt).utf8(rt);
```

---

## 📥 C++ → JavaScript 传值

### C++ 端创建返回对象

```cpp
jsi::Object result(rt);

// 1. 基本类型：number (double → number)
result.setProperty(rt, "width", jsi::Value(width));
result.setProperty(rt, "height", jsi::Value(height));

// 2. 基本类型：boolean (bool → boolean)
result.setProperty(rt, "success", jsi::Value(true));

// 3. 基本类型：string (std::string → string)
result.setProperty(rt, "sizeString", jsi::String::createFromUtf8(rt, sizeStr));

// 4. null 值
result.setProperty(rt, "error", jsi::Value::null());

// 5. 嵌套对象 (jsi::Object → nested object)
jsi::Object sizeInfo(rt);
sizeInfo.setProperty(rt, "width", jsi::Value(width));
sizeInfo.setProperty(rt, "height", jsi::Value(height));
sizeInfo.setProperty(rt, "unit", jsi::String::createFromUtf8(rt, unit));
result.setProperty(rt, "sizeInfo", jsi::Value(rt, sizeInfo));

// 6. 数组 (jsi::Array → array)
jsi::Array dimensions(rt, 2);
dimensions.setValueAtIndex(rt, 0, jsi::Value(width));
dimensions.setValueAtIndex(rt, 1, jsi::Value(height));
result.setProperty(rt, "dimensions", jsi::Value(rt, dimensions));

// 7. 包含多种类型的对象
jsi::Object metadata(rt);
metadata.setProperty(rt, "tag", jsi::Value(tagValue));
metadata.setProperty(rt, "scale", jsi::Value(scale));
metadata.setProperty(rt, "unit", jsi::String::createFromUtf8(rt, unit));
metadata.setProperty(rt, "timestamp", jsi::Value((double)time(nullptr)));
result.setProperty(rt, "metadata", jsi::Value(rt, metadata));

return result;
```

### JavaScript 端接收

```typescript
const result = RNSVGSvgViewModule.getSVGSize(tag, options);

// 1. 基本类型：number
console.log('width:', result.width);        // 150
console.log('height:', result.height);      // 150

// 2. 基本类型：boolean
console.log('success:', result.success);    // true

// 3. 基本类型：string
console.log('sizeString:', result.sizeString);  // "150x150px"

// 4. null 值
console.log('error:', result.error);        // null

// 5. 嵌套对象
console.log('sizeInfo:', result.sizeInfo);
console.log('sizeInfo.width:', result.sizeInfo.width);
console.log('sizeInfo.height:', result.sizeInfo.height);
console.log('sizeInfo.unit:', result.sizeInfo.unit);
console.log('sizeInfo.aspectRatio:', result.sizeInfo.aspectRatio);

// 6. 数组
console.log('dimensions:', result.dimensions);  // [150, 150]

// 7. 对象
console.log('metadata:', result.metadata);
console.log('metadata.tag:', result.metadata.tag);
console.log('metadata.scale:', result.metadata.scale);
console.log('metadata.unit:', result.metadata.unit);
console.log('metadata.timestamp:', result.metadata.timestamp);
```

---

## 📊 类型转换对照表

### JavaScript → C++

| JavaScript 类型 | JSI 类型 | C++ 类型 | 转换方法 |
|-----------------|----------|----------|----------|
| `number` | `jsi::Value` | `int32_t` / `double` | `args[0].asNumber()` |
| `string` | `jsi::Value` | `std::string` | `args[0].asString(rt).utf8(rt)` |
| `boolean` | `jsi::Value` | `bool` | `args[0].getBool()` |
| `object` | `jsi::Value` | `jsi::Object` | `args[0].getObject(rt)` |
| `array` | `jsi::Value` | `jsi::Array` | `args[0].asArray(rt)` |
| `null` / `undefined` | `jsi::Value` | `std::nullopt` | `args[0].isNull()` |

### C++ → JavaScript

| C++ 类型 | JSI 类型 | JavaScript 类型 | 转换方法 |
|----------|----------|-----------------|----------|
| `int32_t` / `double` | `jsi::Value` | `number` | `jsi::Value(42)` |
| `std::string` | `jsi::String` | `string` | `jsi::String::createFromUtf8(rt, "hello")` |
| `bool` | `jsi::Value` | `boolean` | `jsi::Value(true)` |
| `jsi::Object` | `jsi::Object` | `object` | `jsi::Object(rt)` |
| `jsi::Array` | `jsi::Array` | `array` | `jsi::Array(rt, size)` |
| `nullptr` | `jsi::Value` | `null` | `jsi::Value::null()` |

---

## 🔍 完整示例代码

### JavaScript 端

```typescript
const handleGetSVGSize = () => {
  const RNSVGSvgViewModule = TurboModuleRegistry.get('RNSVGSvgViewModule');
  const svgTag = findNodeHandle(svgRef.current) || 1;
  
  // JavaScript → C++ 传值
  const options = {
    unit: 'px',                    // string
    scale: 1.5,                    // number
    includeAspectRatio: true,      // boolean
    extraInfo: ['info1', 'info2'],  // array
    metadata: {                     // nested object
      source: 'react-native-svg',
      version: '1.0.0'
    }
  };
  
  // 调用 JSI 接口
  const result = RNSVGSvgViewModule.getSVGSize(svgTag, options);
  
  // C++ → JavaScript 传值接收
  console.log('Result:', result);
  console.log('Width:', result.width);           // number
  console.log('Height:', result.height);         // number
  console.log('Success:', result.success);       // boolean
  console.log('SizeString:', result.sizeString); // string
  console.log('SizeInfo:', result.sizeInfo);     // nested object
  console.log('Dimensions:', result.dimensions); // array
  console.log('Metadata:', result.metadata);     // object
};
```

### C++ 端（桥接函数）

```cpp
static jsi::Value __hostFunction_RNSVGSvgViewModule_getSVGSize(
    jsi::Runtime &rt,
    react::TurboModule &turboModule,
    const jsi::Value *args,
    size_t count) {
    
    // JavaScript → C++ 传值解析
    auto tag = args[0].asNumber();                    // number → int32_t
    auto options = args[1].getObject(rt);            // object → jsi::Object
    
    // 调用 C++ 函数
    auto result = static_cast<RNSVGSvgViewModule *>(&turboModule)
        ->getSVGSize(rt, tag, options);
    
    // C++ → JavaScript 传值（返回）
    return jsi::Value(rt, result);
}
```

### C++ 端（实现函数）

```cpp
jsi::Object RNSVGSvgViewModule::getSVGSize(jsi::Runtime &rt, Int32_t tag, Object options) {
    // JavaScript → C++ 传值读取
    int32_t tagValue = *tag;
    jsi::Object opts = *options;
    std::string unit = opts.getProperty(rt, "unit").asString(rt).utf8(rt);
    double scale = opts.getProperty(rt, "scale").asNumber();
    bool includeAR = opts.getProperty(rt, "includeAspectRatio").getBool();
    
    // 业务逻辑...
    
    // C++ → JavaScript 传值创建
    jsi::Object result(rt);
    result.setProperty(rt, "width", jsi::Value(width));
    result.setProperty(rt, "height", jsi::Value(height));
    result.setProperty(rt, "success", jsi::Value(true));
    result.setProperty(rt, "sizeString", jsi::String::createFromUtf8(rt, sizeStr));
    
    return result;
}
```

---

## 🎯 关键知识点

### 1. 参数解析顺序

1. **桥接函数**：解析 `args` 数组
2. **C++ 函数**：从 `jsi::Object` 读取属性
3. **类型转换**：使用对应的转换方法

### 2. 返回值创建顺序

1. **创建对象**：`jsi::Object result(rt)`
2. **设置属性**：`result.setProperty(rt, "key", value)`
3. **返回对象**：`return result`

### 3. 嵌套对象处理

```cpp
// 创建嵌套对象
jsi::Object nested(rt);
nested.setProperty(rt, "key", jsi::Value(42));

// 将嵌套对象添加到父对象
parent.setProperty(rt, "nested", jsi::Value(rt, nested));
```

### 4. 数组处理

```cpp
// 创建数组
jsi::Array arr(rt, size);

// 设置数组元素
arr.setValueAtIndex(rt, 0, jsi::Value(42));

// 将数组添加到对象
result.setProperty(rt, "array", jsi::Value(rt, arr));
```

---

## 📝 日志查看

### C++ 日志

```bash
# 查看所有 getSVGSize 相关日志
adb logcat | grep "\[getSVGSize"

# 查看 JavaScript → C++ 传值日志
adb logcat | grep "JavaScript → C++"

# 查看 C++ → JavaScript 传值日志
adb logcat | grep "C++ → JavaScript"
```

### JavaScript 日志

在控制台查看：
```javascript
console.log('[App] ========== JavaScript → C++ Values ==========');
console.log('[App] ========== C++ → JavaScript Values ==========');
```

---

## 🎓 学习要点

通过这个增强版示例，你可以学习到：

1. ✅ **完整的双向传值流程**
   - JavaScript → C++：参数解析和类型转换
   - C++ → JavaScript：返回值创建和类型转换

2. ✅ **各种数据类型的处理**
   - 基本类型：number, string, boolean, null
   - 复合类型：object, array, nested object

3. ✅ **类型转换方法**
   - JavaScript → C++：`asNumber()`, `asString()`, `getBool()`, `getObject()`, `asArray()`
   - C++ → JavaScript：`jsi::Value()`, `jsi::String::createFromUtf8()`, `jsi::Object()`, `jsi::Array()`

4. ✅ **嵌套数据结构**
   - 嵌套对象的创建和访问
   - 数组的创建和填充

---

**文档版本**: 1.0  
**最后更新**: 2024-12-19  
**适用项目**: rntpc_ios

